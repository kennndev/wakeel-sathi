{
  "name": "wakeel-sathi",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "node --env-file=.env ./node_modules/next/dist/bin/next dev apps/chamber-web",
    "build": "node --env-file=.env ./node_modules/next/dist/bin/next build apps/chamber-web",
    "lint": "npm run lint -w apps/chamber-web",
    "typecheck": "npm run typecheck -w apps/chamber-web"
  }
}
